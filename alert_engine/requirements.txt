# Install deps from repo root: pip install -r requirements.txt
# (Do NOT run this file directly from repo root — ../python is relative to this folder.)
beautifulsoup4>=4.12
curl_cffi>=0.7
fastapi>=0.110
openpyxl>=3.1
python-dotenv>=1.0
requests>=2.31
uvicorn[standard]>=0.27
../python
